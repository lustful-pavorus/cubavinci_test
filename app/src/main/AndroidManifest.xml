<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cu.esudev.cubavinci">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CubaVinci">
        <activity android:name=".TestActivity"></activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AsignatureActivity" />
        <activity android:name=".TOEActivity" />
        <activity android:name=".TestsActivity" />
        <activity android:name=".ExerciseActivity" />
        <activity android:name=".ExercisesActivity" />
        <activity android:name=".TopicsActivity" />
    </application>

</manifest>